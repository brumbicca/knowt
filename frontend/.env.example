# Chave enviada no header X-Fiesta-Bi-Key (deve coincidir com o mapa nginx na VPS)
VITE_BI_BRIDGE_KEY=change-me

# Em produção o path é same-origin. Em dev local, aponta ao proxy Vite ou URL absoluta.
# VITE_BI_BRIDGE_BASE=/api/bridge
